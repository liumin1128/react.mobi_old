import React, { PureComponent } from 'react';

export default class LoginModal extends PureComponent {
  state = {
    mode: 'up',
  }

  render() {
    return (
      <div>
        8
      </div>
    );
  }
}
